{% extends 'base.html.twig' %}
{% block content %}
<div class="container text-center situation_pro tab step" id="3-content">
  <div class="row" id="subStepProjectOffers">
    <div class="title">
      <h2 class="fw-light">
        Mon projet
      </h2>
    </div>
    {{ form_start(form) }}
    <div class="step1_1A_project row">
      {{ form_row(form.projectType) }}
    </div>
    <div class="btnForm">
      <div>
        <button type="button" class="btnPrev">Précédent</button>
        {{ form_widget(form.submit) }}
      </div>
    </div>
    {{ form_end(form) }}
  </div>
</div>
{% endblock %}
