{% extends 'base.html.twig' %}
{% block content %}
<div class="container text-center situation_pro tab subStepCoBorrower step" id="10-content">
  <div class="row" id="situationProfessionalCo">
    <div class="title">
      <h2 class="fw-light">
        Situation professionnelle co-emprunteur
      </h2>
    </div>
    {{ form_start(form) }}
    <div class="row step1_1A_project conso_label">
      {{ form_row(form.professionalSectorCo) }}
      {{ form_row(form.professionalTypeCo) }}
      {{ form_row(form.professionalContractTypeCo) }}
      {{ form_row(form.professionCo) }}
      {{ form_row(form.daySecteurCo) }}
      {{ form_row(form.monthSecteurCo) }}
      {{ form_row(form.yearSecteurCo) }}
    </div>
    <div class="btnForm">
      <div>
        <button type="button" class="btnPrev">Précédent</button>
        <button type="submit" class="btnNext">Suivant</button>
      </div>
    </div>
    {{ form_end(form) }}
  </div>
</div>
{% endblock %}
